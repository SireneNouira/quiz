document.addEventListener('DOMContentLoaded', () => {
    console.log(quizData.id);  // ID de la question
    console.log(quizData.question_text);  // Texte de la question
    console.log(quizData.answers);  // Tableau des réponses
});


